import { ADD_INVOICE } from "../actions/types";

const initialState = {
  invoices: [],
};

const reducer = ((state = initialState), action)=>{
    switch (action.type) {
        case ADD_INVOICE:
            
            break;
    
        default:
            break;
    }
}
